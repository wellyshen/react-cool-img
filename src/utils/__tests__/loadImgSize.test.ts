/* import loadImgSize from '../loadImgSize';
import loadingImg from '../../App/images/loading.gif';

it('test', done => {
  const cb = (error: any, data: { width: string; height: string }): void => {
    expect(error).toBe(null);
    expect(data.width).toBe('252');
    expect(data.height).toBe('252');
    done();
  };

  loadImgSize(loadingImg, null, null, cb);
}); */

it('mock test', () => {});
